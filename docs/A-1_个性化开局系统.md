# A-1 个性化开局系统（五维专属剧情 + 八理想线）

> 批次：超大型内容扩充 · 批 A-1 ｜ 日期：2026-09-09 ｜ 状态：已交付
> 口令依据：《群雄割据 超大型内容扩充》批 A-1 定义与验收标准

## 一、目标

让「不同的搭配开局有对应的剧情和走向」：职业 × 理想 × 爱好 × 天资 × 亚种五维组合，在序章开场产生专属文本；并为 8 条理想目标线建立可长期挂载的里程碑链（后续批次 B/C 可扩展）。

## 二、扩充了什么（改动清单）

### 1. 新增数据文件

| 文件 | 内容 | 大小 |
|---|---|---|
| `src\data_nodes\dn_origin_profile.js` | `ORIGIN_PROFILE`：identity（9 故乡身份）/ job_sight（9 职业见闻）/ ideal_reaction（8 理想第一反应）/ hobby_note（10 爱好）/ talent_judge（4 天资）/ subrace_note（7 族系血脉注脚）/ subrace_to_family（30 亚种→族系映射）/ `_build(S)` 只读组装函数 | 7,431 B |
| `src\data_nodes\dn_ideal_goals.js` | 8 条理想线 ×（intro + 3 里程碑）= **32 节点**，对象式，带 `tag/place/pace`；intro 确认时置 `S.flags.ideal_goal_<ideal>`；`_3` 完成时置 `_done` 并 `go:"fc_tavern"` | 11,677 B |

### 2. 引擎最小侵入（src\script_03.js，共 5 处）

| 位置 | 改动 | 语义 |
|---|---|---|
| `v91_resolveText` node 级 | 新增五键变体 `ifJob/ifIdeal/ifHobby/ifTalent/ifSubrace`（插在 ifRelation 判定后、return 前） | 读 S 五维键选段，与 ifFlag 同构，向后兼容零回归 |
| `v91_resolveText` raw 级 | 同上，raw 对象内嵌形态 | 同上 |
| writeNext 正文渲染前 | 新增 `window.v91_originProfile(node,txt)` 注入钩子（记忆注入之后） | 序章前 3 个节点各注入 1 段五维专属文本；`S.flags.origin_profile_done` 置位或 3 段用尽后零注入 |
| `applyDefaults` | `if(s.settings.originProfile===undefined) s.settings.originProfile=true;` | 独立键兜底，旧档兼容 |

### 3. 蓝图与账本登记

- `dn_story_blueprint.js` nodeIndex：尾部锚点法插入 32 节点（`goal_intro_*` + `goal_*_1/2/3`），归属 `vol_free / act1 / ch_free_jiaohui`，语法校验通过。
- `dn_causality.js`：新增 8 条账本项 `led_a1_01~08`（type:设定，plant=goal_intro_*，reap=goal_*_3，status:open）。账本总数 49 项。

### 4. 未改动

- 判定公式 / writeNext 核心语义 / choose / 存档结构语义：**零触碰**。
- saveVersion=48 不变；applyDefaults 兜底链不变。
- 既有节点内容与选项：零改动。

## 三、为什么（设计依据）

- 五维变体复用 CM-2 已落地的 `v91_resolveText` 三形态解析，不新增引擎逻辑，只扩展五个读键——满足「新增内容零引擎改动」红线。
- 注入钩子放在记忆注入之后、渲染之前，只读 S，失败即吞异常返回 null——最小侵入。
- 32 节点全部对象式、带 pace 元数据，与 SP 流水线 schema 一致，可直接被 elda-ci 检查器覆盖。

## 四、如何验证（已执行）

| 验证项 | 结果 |
|---|---|
| `node --check`（26 块 src） | PASS |
| `_build_authority.py build` → Copy → verify → sync | PASS（四路字节一致：game=6864846B / chunked=3732777B） |
| `elda ci` 24 检查器 | **全部通过**（设定深度/UI/V68/账本 49 项/节奏/文本治理/叙事骨架 3937 索引/弧线覆盖 90.4%/生产报表/性能预算） |
| 性能预算门 | 因新增内容合法超基线，按口令 `elda budget --reason A1_20260909` 更新 baseline（6819424→6864846），重跑全绿 |
| `smoke_test.py` | PASS（30 步推进 / 5 面板 / 存读档 / 结局可达） |
| 死链 | 0 |

## 五、入口衔接（后续批次）

- 理想线入口挂载：批 A-2 自由城 fc 链新增「想起自己的理想」选项（按 `S.ideal` 用 ifIdeal 变体显示对应选项，go 到 `goal_intro_<ideal>`）。
- 里程碑 `_3` 完成后 `go:"fc_tavern"` 回到自由城主线，与 A-2 汇流段衔接。
- 备份快照：`backup\snap_20260909_225808_A1_20260909.zip`（改动前）；本次改动后未再备份（改动均验证通过后落盘）。

## 六、验收对照

- 五维搭配专属开场：✓ 已实现（注入钩子 + 数据表），组合实测并入 E-1 整合回归。
- 8 理想目标线全部可触发：✓ 节点与 flag 链齐备（入口在 A-2）。
- 蓝图 nodeIndex 标注：✓ 32 项；账本登记：✓ 8 项。
