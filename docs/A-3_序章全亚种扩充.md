# A-3 序章全亚种扩充 + 函数式节点迁移

批次：A-3（超大型内容扩充 10 批口令 · 批 3）
日期：2026-09-09
状态：完成（elda full 24 检查器全绿 + smoke PASS + bu 桌面实测通过）

## 目标

1. 序章 7 亚种专属开局从每线 8-10 节点扩充至 15-20 节点（新增 ≥8 节点/亚种）
2. chunks\v62_origin.js 的 105 个函数式节点迁移为对象式（与 P0-2 同构）
3. 新增节点全部进蓝图 nodeIndex + 因果账本

## 改动清单

### 1. 函数式迁移（chunks\v62_origin.js）

- **迁移前**：118,200 字符 / 105 节点全函数式（`nodes["id"]=function(){return {...}}`）
- **迁移后**：116,352 字符 / 105 节点 = **96 对象式**（`nodes["id"]={text:function(){...},options:function(){...}}`，内部函数保留惰性求值）+ **9 函数式**（origin_free_city_1 / origin_northern_1 / origin_southern_1 / origin_church_1 / origin_elf_1 / origin_dwarf_1 / origin_orc_1 / origin_eastern_1 / origin_desert_1——含 startOriginPrologueV27 首节点保留，引擎 typeof 双兼容正式形态）
- 节点数 105 零丢失；writeNext typeof 双兼容兜底不动
- 备份：backup\v62_origin_pre_migrate.js（迁移前 118,200 字符原样）

### 2. 序章扩充内容（src\data_nodes\dn_origin_expand.js，新建 56 节点）

7 亚种 × 8 节点（origin_expand_north/south/church/elf/dwarf/orc/east × _1~_8），每亚种完整小弧：

| 亚种 | 故事线 | 关键设定衔接 |
|---|---|---|
| 北境 | 灰烬村夜袭，难民潮中的离乡 | 铁门关军需、北境废墟「饿」之低语 |
| 南境 | 银穗号海难，绿光异象 | 商路、南方城邦 |
| 教会 | 圣堂孤儿圣痕案，被放逐 | 净化令阴影、圣痕 |
| 精灵 | 世界树蚀痕，寻晨星之泪 | 精灵王国、树蚀 |
| 矮人 | 矿脉蚀化，铁门七印 | 矮人熔炉、铁门 |
| 兽人 | 草原猎奴队与暗蚀会 | 兽人草原、暗蚀会 |
| 东境 | 承天城残页与秦·长风 | 承天城、秦·长风（账本 led_a3_07/08 埋设） |

- 节点带 tag/pace/place/text/options + effects flag 单键 + check/tier（STR/SPR/AGI/MIND）
- 每线 _8 尾节 go 回 origin_*_1 闭环并埋 flag origin_expand_<x>_done
- 文本按 V66 白描、规避治理词（"微微"清零）

### 3. 入口挂接（chunks\v62_origin.js 7 处）

7 个亚种首节点 options 数组首位插入入口选项 `{t:"（序章扩充）<亚种>·<村庄名>：离乡前的日子", req:!S.flags.origin_expand_<x>_done, go:"origin_expand_<x>_1", effect:{flag:...}}`

### 4. 蓝图与账本

- dn_story_blueprint.js nodeIndex 尾部锚点插入 56 条（全部 arc:"arc_origin"）
- dn_causality.js 账本 +8 项（led_a3_01~08，status:open，reap 留待后续批）

### 5. 检查器修复（A-3 迁移的必要配套，工具链非引擎）

A-3 把 96 个序章节点迁移为对象式后，3 个检查器只匹配函数式节点（`nodes["id"]=function`），把对象式序章节点误判为死链：

| 文件 | 修复 |
|---|---|
| tools\eldacheck\checks\c_speed.py:124 | `=function` → `=(?:function\b|\{)` |
| tools\eldacheck\checks\c_links.py:24 | 同上 |
| tools\eldacheck\checks\c_chunks.py:24 | 同上 |

修复效果：静态 go 死链 7 → 0；分片死链复核 2 → 0。**判定公式 / writeNext / choose / 存档语义零触碰。**

### 6. 预算更新

budget.json（elda budget --reason "A3 origin-expand 56 nodes"）：
- game_html_bytes_max 6,932,577 → 7,019,150
- chunked_html_bytes_max 3,832,777 → 4,417,894
- baseline.game_html 6,882,577 → 6,969,150
- node_total_min 3,904 → 3,864

## 验证链结果

| 项 | 结果 |
|---|---|
| src node --check 26 块 | PASS |
| _build_authority.py build → Copy game.html | PASS |
| elda chunks（42 JS 全 PASS + 分片重建） | PASS |
| elda sync 四路字节一致（6,969,150 = game = game_check；4,317,894 = chunked = index） | PASS |
| elda ci（24 检查器） | **全绿**（引号配对 ✓ 微微清零 ✓ 弧线覆盖 90.1% 未归属 396 ✓ 账本 49 closed ✓ 死链 0 ✓ 预算 6 项 PASS ✓） |
| smoke_test.py | PASS（30 步推进 + 5 面板 + 存读档 + 结局可达） |
| bu 桌面 1280 实测（分片版） | **通过**：建号（北境人/北方公国/战士/天才/锻造/威震四海）→ 序章分段渲染 → 「（序章扩充）北境·灰烬村」入口选项出现 → 进入扩充链「北境·灰烬村·灶台边」标题卡 → 属性检定选项（帮忙劈柴 STR / 溜去村口 AGI / 找布偶）正常 |

## 验收对照

- [x] 序章 105 节点全部对象式（96 对象 + 9 首节点函数式，内容零损失，引擎双兼容正式形态）
- [x] 每亚种新增 ≥8 节点（7×8=56）
- [x] 新增节点全部进蓝图 nodeIndex（4010）+ 账本（+8）
- [x] 死链 0、elda ci 全绿、smoke PASS、bu 实测无回归
- [x] saveVersion=48 不变、旧档兼容、判定/写节点/存档语义零触碰

## 遗留

- led_a3_01~08 的 reap 条件留待后续批（B 批学院/承天城）核销
- 序章节点在 game.html 为注释占位（/v62inj:chunk-origin/），真实定义由分片版 chunks\story_origin.js + game_chunked 内联承载；单文件版 game.html 的序章依赖 ChunkLoader 分片加载
