# CT-2 ~ CT-4 内容工具生态完善（三大路线 · 线一）

> 批次：CT-2/CT-3/CT-4（2026-09-09）｜ 前置：CT-1（图谱标签化 + content stat）已完成
> 本文件含三批的改动清单与扩充记录；CT-1 见 docs\CT-1_内容工具图谱标签化.md。

---

## CT-2 新增节点脚手架（elda content new）

### 改动清单
| 文件 | 改动 |
|---|---|
| `tools\elda\p2tools_impl.py` | 新增 `cmd_content_new` + `_SCAFFOLD_TMPL` + dispatch 注册 `new`；补 `import subprocess` |
| `_build_authority.py` | `_collect_data_nodes()` 排除 `dn_scaffold.js`（脚手架为 dev 模板，不进构建） |

### 产物
- `src\data_nodes\dn_scaffold.js`：脚手架模板文件，含——
  - tag 字段（按 --type 映射 main/branch/event/ending）
  - place/where/pace 骨架（pace 默认 normal）
  - text 数组骨架 + ifFlag/ifRelation 状态感知变体注释（CM-2 三形态示例）
  - options/go 占位（需填写真实目标）
  - CM-3 账本登记提示（埋设/回收伏笔须同步 dn_causality.js）
  - V66 文风 + T0-2 治理词提示 + 战斗四段式提示

### 实测
```
elda content new --type main --id bd2_desert_entry --vol "死亡沙漠深化包"
→ [OK] 脚手架已生成 …；生成即过 node --check
```
产物 7 项要素校验全 OK（pace/ifFlag/ifRelation/账本提示/战斗提示/tag/options）。

### 扩充记录
| 扩充 | 为什么 | 如何验证 |
|---|---|---|
| 脚手架进构建排除 | 占位节点 go 指向占位目标会触发死链 FAIL | elda ci verify 幂等通过、四路字节不变（6221943B） |
| 变体注释内嵌 | 新节点一步到位支持 CM-2 状态感知 | node --check + 要素校验 |

## CT-3 文本治理持续门禁（elda text guard + 第 21 检查器）

### 改动清单
| 文件 | 改动 |
|---|---|
| `tools\elda\p2tools_impl.py` | 新增 `cmd_text_guard` + `_guard_scan_text`（30 词表 ≤30 处 / 引号配对 / 连续标点）+ dispatch |
| `tools\eldacheck\checks\c_textguard.py` | 新检查器（run(html) 扫 game.html 同口径，超阈值 FAIL + 逐词清单） |
| `tools\eldacheck\eldacheck.py` | import + full 列表注册（第 21 检查器） |
| `tools\elda\elda.py` | cmd_ci 文案 20→21 |

### 实测
```
elda text guard
  [PASS] AI 高频词：30 词全 ≤30 处
  [PASS] 中文引号配对：左=1782 右=1782
  [PASS] 连续标点：0 处
  结果：全部通过（防回潮基线保持）

elda full → 21 个检查器全部通过
  文本治理门禁 [PASS] 高频词=0超标 引号配对=是 连续标点=0
```

### 扩充记录
| 扩充 | 为什么 | 如何验证 |
|---|---|---|
| 门禁阈值 ≤30（v91 最高 22） | 防 T0 抽改词回潮，留 8 处余量 | ci 每次发布自动跑 |
| 检查器与命令同口径 | 命令=src 定位清单，检查器=game.html 门禁 | 两侧输出一致（1782 对等） |

## CT-4 工具手册更新

### 改动清单
- `docs\工具手册.md` 全文重写（V59 → V91-CT）：命令一览补 ci/content/text/budget/node/regress/serve 等；检查器表扩至 21 个；新增 content/text 命令族表、node_graph 操作说明、10 分钟脚手架演练、故障表与验证链更新。

### 扩充记录
| 扩充 | 为什么 | 如何验证 |
|---|---|---|
| 10 分钟演练 | 让"新增 1 节点只加数据不改引擎"可操作 | 按演练步骤执行 elda ci 全绿 |

---

## 验证链（CT 线总）
```
elda ci → [PASS] 26 块语法 / 权威源幂等 / 四路字节一致（6221943B=6221943B, 3573097B=3573097B）
         → [PASS] elda full 21 检查器 / 预算门 6 项
bu 实测 node_graph.html：3823 节点 / 6213 关系 / 45 卷，标签过滤 50 节点、卷过滤、搜索定位、重置全通过
```
游戏运行时代码零改动；节点 3,823 零损失；saveVersion=48 不变。
