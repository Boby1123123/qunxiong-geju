# F-5 自由城第二幕 + 旅途事件 + F-3/F-4 入口修复（大型剧情补充 · 批 F-5）

日期：2026-09-10 · 基线：节点 4,689 / 事件池 195 / 账本 166 / saveVersion=48

## 目标

1. **自由城第二幕**：交汇城"再待两日"的生活深描——晨市/镖局/夜酒/离城前夜，补足"城与人的活法"底色（此前 fc 链只到去向选择即离城，缺临行前的驻足感）。
2. **旅途事件**：四去向（北境/西境/沙漠/东境）的旅途风物播报事件，让"在路上"有世界感。
3. **F-3/F-4 入口修复（重要）**：F-3 码头姑娘弧线、F-4 身世暗线的入口此前只靠事件池播报文本（事件不跳节点），玩家实际无法进入——本次在自由城 hub 挂真实选项入口，弧线变可达。

## 改动清单

### 新增文件
- `src\data_nodes\dn_f_travel.js`（新建，4 节点）
  - world_f5_city_1 晨市（退伍兵代写家书——与 F-4 孤儿院呼应）
  - world_f5_city_2 镖局（北境线镖，接下/不接两路；ifFlag f5_escort 后续变体）
  - world_f5_city_3 跛脚酒桶夜话（ifFlag f5_escort：蜜尔娜丈夫的旧怀表——走镖人的遗物，收下即 flag）
  - world_f5_city_4 离城前夜（ifFlag f5_escort 怀表 / f5_city_letter 寄信回响两变体）

### 修改文件
- `src\script_02.js`：fc_jiaohui_entry 选项 +3（去码头→world_f3_lu_1 / 去旧货街→world_f4_blood_1 / 再待两日→world_f5_city_1）——F-3/F-4 弧线入口由此可达，入边 0 孤儿
- `src\script_01.js`：EVENT_POOL_EXT +4（f5_travel_north 115 暴雪 / west 145 元素风暴 / desert 175 绿洲水囊商机 / east 205 官道劫案——day 与五主线错开）
- `src\data_nodes\dn_story_blueprint.js`：nodeIndex 尾部锚点 +4（arc_freedom 自由城第二幕）
- `src\data_nodes\dn_causality.js`：账本 +1（led_f5_city 交汇城人情底色）

## 设计要点

1. **入口修复优先**：F-3/F-4 的节点此前无入边（事件池只播报不跳转）——hub 挂载后，码头姑娘线（F-3 lu）、身世暗线（F-4 blood）从"广播里的传闻"变成"可主动造访的事"
2. **生活化第二幕**：把交汇城从"中转站"变成"有记忆的城"——代写家书的退伍兵、走镖遗物怀表、晨市的粮价与船期；三个 flag（f5_city_letter/f5_escort/f5_city_done）为后续旅途/终局提供回指材料
3. **变体互锁**：city_4 的离城文本按玩家选择（接镖/寄信）呈现不同收束，与 F-1/F-2 的"抉择余波"手法一致
4. **旅途事件纯播报**：四则旅途风物事件与 BD 包呼应（西境元素风暴↔hstorm、沙漠↔desert_lvzhou），不改任何判定

## 踩坑与修复

1. **hub 插入锚点踩坑（反复 3 次）**：`go:"fc_guild"}` 在文件多处出现（酒馆/公会节点都有）→ 第一次 find 命中 105 行前段，插入破坏结构；第二次用 `N["fc_jiaohui_entry"]` 做 find 又命中**引用字符串**而非定义（该 id 在别处被提及）；最终用"定义锚 + 段内下一个选项起点"双定位，精确插到 `go:"fc_guild"},` 与 `{t:"跟上前方..."}` 之间才成功。**教训：大文件改选项块必须用"节点定义起点 + 段内唯一锚 + 下一选项起点"三级定位**
2. **ASCII 引号闭合再犯**（F-3/F-4 同型第三次）：city_3 蜜尔娜文本行尾 `。` 后漏 `"`——自动修复脚本字符类未含中文右引号 `”`（行尾是 `”` 而非中文句读），漏修；手修后过。**教训：闭合检查必须覆盖行尾为 `”`/`’`/`』` 的情况**
3. **wire 脚本重跑污染**：修复 hub 时旧 wire 脚本（错误锚点版）被重跑，二次破坏 script_02.js——git checkout 恢复后只跑精确版。**教训：临时 wire 脚本失败后必须改名/删除再迭代，禁止重跑旧版**
4. 预算门：game.html 8,656,836B > baseline → `elda budget --reason` 更新后 ci 全绿

## 验证链结果

- node --check（26 块）PASS；text guard 全绿；content dup 2,609 源定义（90 文件）重复 0；chain 无新增死链
- build → chunks → sync：四路字节一致 game=8,656,836B / chunked=5,888,623B
- elda full 25 检查器 PASS（节点 4,693、V66 PASS、c_causality PASS）
- smoke_test.py PASS
- bu 桌面实测：reload 后 N 2996 keys、4 新节点全注册；fc_jiaohui_entry hub 现 8 选项（新 3 + 旧 5 完整保留）；city_1 渲染晨市；city_4 f5_escort 变体显示"怀表" ✓

## 验收对照

- [x] 自由城第二幕 4 节点全可达（hub 挂载）；F-3/F-4 弧线入口修复（入边 0 孤儿）
- [x] 旅途事件 4 则并入池（day 115/145/175/205 与五主线错开）
- [x] 蓝图 +4、账本 +1（led_f5_city）
- [x] saveVersion=48 不变、旧档兼容、节点只增不降（4,693）、elda ci 全绿、smoke PASS、bu 无回归

## 后续衔接

- F-6：结局互锁 + 战役战斗（美第奇/金秤双线收束进终局；f5_escort 北境镖可作为战争线入口之一）
