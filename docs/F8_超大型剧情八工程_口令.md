# 【口令 · 群雄割据 超大型剧情八工程（F/E/C/S/T/L/P/W）】

请基于你对《艾尔达大陆·群雄割据》的完整知识（世界观、设定、src 权威源、引擎、S 状态、工具链、构建关系、v93+N 系列现状、docs 体系、elda-* 技能），制定并实施"超大型剧情八工程"——以爆火文游（Choice of Games / 极乐迪斯科 / 人生重开模拟器）的设计共性为蓝本，完成失败开门、多年回响、主线聚合、人格声音、心念殿、线索拼合、情感搭档、事件扩充八线深度改造。

项目根目录：`D:\1pao tuan\群雄割据`

## 依据文档

- 本口令为唯一内容蓝图（来源于联网调研：CoG 玩家口碑——微小选择多年回响/失败开蒙羞线/分支狂但不裂主线；极乐迪斯科——24 技能人格化插话/思绪内阁/失败和成功一样有趣/金·曷城式情感脊梁；人生重开模拟器——海量事件×天赋协同/阶段解锁/社交分享）
- `docs\N_叙事人格革命_口令.md`（N-1~N-8 已落地：立场轴/对白记忆/结局回响/卡牌册/五维好感/世界图书馆/天赋连锁/名场面）
- `docs\CM-3_因果账本.md`（账本 169 项 closed 148）、`docs\CM-4_节奏字数.md`（pace 门禁）
- `docs\世界设定_v64.md`（8 势力/五主线/金秤/晨天/七锚/铁牌/神谕）
- `docs\N-2_文本本体革命_超大型更新.md`（TQ/CON/DEEP/EXP 已落地）
- `docs\权威源与构建链_v69.md`、`docs\状态Schema_v70.md`、`docs\存档层_v71.md`

## 引擎锚点（改动前复核，以实际为准）

- writeNext 注入链 = `src\script_03.js`（v91_memoryInjection → n1inj → … → n8inj，新增钩子在其后）；applyDefaults 兜底锚 = `script_03.js:75`（`if(!s.worldState) s.worldState={};` 后）
- choose 判定 = script_02.js 核心区（**不触碰**）；changeRelation = `script_02c.js:425`（被 v93n8_rel5Shift 包装于 :20516，原文 `_origCR` 保留）
- 事件池 EVENT_POOL_EXT = `src\script_01.js:19`（176 则，cls 四类：天灾/奇遇/商机/人祸，数组末项逗号铁律）
- 五主线 WORLD_EVENTS = `src\script_01.js:17`（purge day60 / silver 120 / seal 200 / academy 250 / orc 280，**判定零改动**）
- REGIONS 九域 = `src\script_01.js:15`
- 账本 = `src\data_nodes\dn_causality.js`（169 项 closed 148）；蓝图 nodeIndex = `src\data_nodes\dn_story_blueprint.js`（尾部锚点法）
- 变体解析 = `v91_resolveText`（ifFlag/ifRelation 三形态，CM-2 已落地）；记忆注入模板 = `src\data_nodes\dn_memory_tpl.js`（CM-1 六类触发）
- 线索账 = CLUE_TRACKS（N-3，3 链 12 probes）；天赋连锁 = `src\data_nodes\dn_combo.js` + `dn_combo_lines.js`（N-7，10 条 + 3 隐藏线）
- 图鉴 tabs = `script_05.js`（当前 9 项）
- 构建链 = `node --check` → `_build_authority.py build` → Copy game_built.html → game.html → `elda chunks` → `elda sync` → `elda budget --reason` → `elda ci`（39 检查器）→ `smoke_test.py`

## 当前基线（改动前对照）

- 节点 4,869 / 事件池 176 / 账本 169 closed 148 / ci 39 检查器 / saveVersion=48
- 四路：game=game_check=9,029,702B、chunked=index=6,274,172B
- 目标：节点 → ≥5,600（+~750）；事件 → ≥300（+~130）；文本增量 40-60 万字

---

## 执行范围（八工程 18 批；批次间不停等，全部做完，内容不缩水）

### 【工程 F · 失败开门】（3 批）—— 灵感：极乐迪斯科"失败和成功一样有趣" / CoG 官方认可的"disgraced 蒙羞秘密路线"

批 F-1 · 失败专线骨架
- 扫描全部 tag=combat 及关键判定节点，识别 `tier.fail` 现状（哪些失败后"损失+原路继续"、哪些已开路）
- 新增数据文件 `src\data_nodes\dn_failpath.js`：登记首批 15 个关键判定节点 → 失败专线映射（fail 后 go 到专线，不走原路）
- 失败专线 = 3-5 节点闭环（蒙羞离城→遇见正常路线见不到的人→别有天地的际遇），每条专线带 pace 字段、进蓝图 nodeIndex、涉及设定同步账本
- 首批 15 条失败专线全部可触发、可闭环；选项集与判定公式零改动（只改 tier.fail 的 go 目标与失败正文）

批 F-2 · 失败专线大规模落地
- 再补 15-25 个关键判定节点的失败专线（累计 30-40 条）
- 专线人物池：罪犯/异端/被遗忘者/黑市商人等"正常路线见不到的人"，与既有支线互挂（失败后才可见的支线 ≥3 条，闭环 ≥4 节点，沿用 changeRelation 里程碑）
- 与教会审判、七锚线索、势力声望联动：失败可导致声望反转但打开地下信息渠道

批 F-3 · 失败回响 + 蒙羞身份
- 失败 flag 全部登记"回响账本"（见工程 E 机制，复用）：失败在数年后以"翻盘"或"反噬"形式回响，≥15 处回响场景
- 新增 `S.failRep` 独立键（applyDefaults 兜底，旧档兼容）：记录蒙羞程度；状态感知文本（v91_resolveText 变体）——蒙羞者在后续对话/事件中被人认出的专属正文
- 蒙羞专属小支线 1-2 条（"用另一种方式证明自己"）

### 【工程 E · 多年回响】（2 批）—— 灵感：CoG"离别礼物数年后出现" / 《崩坏之兆饥荒》"第一章的决定被记住"

批 E-1 · 回响账本登记
- 新增数据文件 `src\data_nodes\dn_echo.js`：100+ 微小选择登记（序章带的信物/对路人的态度/学院选修课/自由城帮助过的人/交易中多给的铜子……），每条 `{id, flag, echoAt(时刻:毕业|战争|终局|特定节点), tpl[]}`
- 与 CM-3 账本衔接（重大项同步 dn_causality.js，轻量项只进 dn_echo.js）；登记即校验 flag 真实存在（elda content chain 不新增死链）
- 回响时刻表与时间线一致（不改变 day 推进与主线判定）

批 E-2 · 回响场景落地
- 40-50 个回响场景：毕业、战争、终局、七锚集齐等时刻触发专属正文（v91_resolveText 变体 + 记忆注入 v2 带出回响句）
- 回响不改变判定、不改选项集——只改变正文与可感知细节；每场景带 pace 字段、进蓝图 nodeIndex

### 【工程 C · 主线聚合】（2 批）—— 灵感：CoG"分支狂多但不裂主线" / 极乐迪斯科"tribunal 无论怎么走都会发生"

批 C-1 · 聚合枢纽设计
- 五主线（purge/silver/seal/academy/orc）+ 七锚终局（anchor_finale_*）+ 战争线（warphase_*）设计 10-15 个聚合枢纽节点
- 聚合枢纽 = 状态感知文本：不同路径到达时正文不同（回忆自己走过的路、见到的人、放下的仇怨），复用 v91_resolveText 三形态；主线判定零改动
- 设计文档 `docs\C-1_聚合枢纽设计.md`：每个枢纽列出上游路径清单 + 各自变体文本要点

批 C-2 · 聚合枢纽落地
- 全部枢纽节点写作落地（各 400-800 字，deep 为主），带 pace、进蓝图 nodeIndex、涉及设定同步账本
- 典型：终局三择（封印/解封/超脱）前加"回顾"聚合节点——按玩家集齐七锚的路径、立场轴、蒙羞经历给出不同引文
- bu 实测：两条不同路径到达同一枢纽，正文不同、判定一致

### 【工程 S · 人格声音】（3 批）—— 灵感：极乐迪斯科 24 技能人格化插话（"技能是故事引擎，不是数值"）

批 S-1 · 声音引擎钩子
- writeNext 注入链新增 n9 钩子 `v93s1_voiceInjection()`（在 n1~n8 之后，最小侵入，不触碰 writeNext 核心语义）
- 14 个内在声音定义：六主属性（STR/CON/AGI/INT/SPR/CHA）+ 8 核心技能（bargain/lore/persu/stealth/detect/survival/martial/medical 或按既有技能体系取实际）
- 开关 `S.settings.voiceLine` 默认 true（applyDefaults 兜底）；关闭后与 v93 渲染逐字节一致；注入文本 console 前缀 `/v93s1:` 可审计
- 声音权重规则：属性等级越高该声音越响（出现频率越高、语气越自信）；全部只读 S 状态，零写入

批 S-2 · 插话模板库
- 新增数据文件 `src\data_nodes\dn_voices.js`：14 声音 × 情境分类（战斗/社交/探索/交易/危险/希望/绝望）各 2-4 条模板 = 300-400 条
- 每条按 V66 文风书写（30 治理词约束、中文引号成对、禁 AI 高频词）；每个声音有独立口吻（如 INT 冷静考据、CHA 世故圆滑、SPR 直觉玄想、AGI 急躁利落）
- 与记忆注入 v2 共存：声音插话与记忆注入分别成段，不互写

批 S-3 · 声音冲突 + 专属选项
- 双高属性冲突时"声音吵架"（内心议会式 2-3 句交锋，非高频触发，单节点 ≤1 次）
- 属性达标解锁该属性的专属对话选项（复用既有 check 机制，补专属文案与 go）
- 与工程 T 心念殿联动：内化概念后相关声音获得专属插话

### 【工程 T · 心念殿】（2 批）—— 灵感：极乐迪斯科 Thought Cabinet（"思绪内阁"）

批 T-1 · 心念殿系统
- 新增数据文件 `src\data_nodes\dn_thoughts.js`：20-30 个可内化概念（七锚真相/金秤守门人身份/神谕含义/兽人战争观/教会审判逻辑/晨天故都秘史/自由城商道哲学……），每条 `{id, concept, source(获取节点/事件/藏书), cost, buff, debuff, unlockFlags[]}`
- 机制：接触概念（对话/事件/文档/世界图书馆藏书触发）→ 玩家选择"内化"→ 内化期（若干行动点/天数）挂 buff/debuff → 完成后永久加成并解锁该概念专属对话选项与正文变体
- 全部独立键（S.thoughts 数组 + S.settings 开关，applyDefaults 兜底，旧档兼容）；不改变判定公式与存档结构

批 T-2 · 心念殿内容落地
- 概念获取节点全量写作（每概念 2-4 节点：接触→犹豫→内化抉择），带 pace、进蓝图 nodeIndex、涉及设定同步账本
- 内化后专属内容：≥20 处解锁对话/正文变体（v91_resolveText ifFlag）；与 N-1 八理想立场轴联动（立场决定哪些概念"容易内化"，立场轴 3 段立场文本复用）
- 与 N-6 世界图书馆联动：藏书中的线索块可作为概念来源

### 【工程 L · 线索拼合】（2 批）—— 灵感：《奥伯拉丁的回归》跨场景拼合 / 九游"公平推理" / 《彼方的她》信息双重盲区

批 L-1 · 线索碎片大规模化
- CLUE_TRACKS（N-3，3 链）扩展为多条线索链：金秤/七锚/神谕三大谜题各拆 8-12 个线索碎片 = 30+ 碎片
- 碎片分藏：不同对话、事件、世界图书馆藏书、冒险手记、人物好感线；全部前期可接触（公平推理原则：不后期追加关键信息）
- 每碎片登记进 dn_clue.js（或既有线索数据文件扩展）；与账本衔接

批 L-2 · 拼合枢纽
- 3 个"拼合"节点：玩家到达特定地点/时刻可"拼合线索"——按已集齐数量给不同正文（0-3 条茫然/4-7 条隐约/全齐真相大白）
- 拼合结果影响后续对话选项（ifFlag 解锁，不改变主线判定）；拼合枢纽带 pace、进蓝图 nodeIndex
- 信息盲区细节：玩家知道的事 NPC 不知道（反之亦然）在对话中体现——NPC 对话按玩家线索进度给不同回应

### 【工程 P · 情感搭档】（2 批，非队伍系统）—— 灵感：极乐迪斯科金·曷城（"游戏最大惩罚是搭档失望的眼神"）

> ⚠️ 边界声明：本工程为**单线剧情角色**，不是可招募的同伴/队伍系统——不改变战斗/行动系统、不新增随行机制、不进入队伍界面。用户红线"同伴队伍系统禁止制作"不触碰；如需调整边界，先向用户确认。

批 P-1 · 搭档确立
- 选定 1 个固定剧情搭档（贯穿序章→自由城→学院→毕业→战争→终局；建议：学院同窗或老铁，与既有好感线深度绑定，改动前确认人选）
- 新增 `S.trust` 独立键（搭档信任/失望维度，applyDefaults 兜底；与五维好感并存不互写）
- 搭档行为规则表：哪些行动涨/跌信任（写进 dn_partner.js 数据文件）
- 设计文档 `docs\P-1_情感搭档设计.md`：搭档人设/信任阈值/关键时刻清单

批 P-2 · 搭档落地
- 20+ 互动节点：相遇/同行关键场景/分歧/和解/生死时刻，全部 deep 化（400-800 字）
- 关键时刻"无声评判"：搭档不说话，只有神态描写（"他把目光移开了""他按了按剑柄，什么都没说"）——信任高低给不同神态正文（v91_resolveText 变体）
- 与工程 E 回响联动：搭档记得玩家数年前的选择；与工程 F 联动：玩家蒙羞时搭档的处置

### 【工程 W · 事件扩充 + 阶段解锁】（2 批）—— 灵感：人生重开模拟器 1500 事件池×天赋协同 / "生命周期短→阶段变化解锁新机制"

批 W-1 · 事件池扩充（一）
- EVENT_POOL_EXT 新增 65 则（累计 176→241）：全部微叙事化（3-5 句：环境+在场+后果），day 与五主线（60/120/200/250/280）错开 ≥15 天，cls 四类均衡，数组末项逗号铁律
- 事件文本与既有节点互文呼应（引用已有人物/事件）；世界观合规（8 势力/五主线/金秤/晨天/七锚不冲突）

批 W-2 · 事件池扩充（二）+ 阶段门控
- 再增 65 则（累计 →≥300）；阶段专属事件类型：学院期/毕业期/战争期/终局期各有专属事件池（按 S.gradPath/S.worldWar 等状态门控触发，读 S 零写入）
- 天赋协同隐藏事件链：在 N-7 天赋连锁基础上，事件可与天赋组合出隐藏链（≥3 条，可重复验证）；新事件全部进账本核销校验（elda content causality）

---

## 横向增强（随批落地，全部独立键/独立文件，不碰红线）

- S 新键（全部 applyDefaults 兜底，旧档兼容）：`S.failRep`（蒙羞程度）/ `S.thoughts`（心念殿）/ `S.trust`（搭档信任）/ `S.settings.voiceLine`（人格声音开关）
- 新数据文件：`dn_failpath.js` / `dn_echo.js` / `dn_voices.js` / `dn_thoughts.js` / `dn_partner.js`（如 P-1 定案）
- 账本：新伏笔/回收全部同步 dn_causality.js（169 → 190+，closed 比例保持）；线索碎片进 CLUE_TRACKS 扩展
- 蓝图：全部新节点进 nodeIndex（尾部锚点法），弧内节点归属率门禁保持
- 事件：day 与五主线错开；EVENT_POOL_EXT 数组末项逗号
- 战斗：新增 combat 节点全部四段式（蓄势/交锋/受创·来源+部位+程度+属性影响/转折），过 c_combat 校验

## 铁律（全程，沿用阶段二 + 项目红线）

1. **不触碰**：判定公式 / writeNext 核心语义 / choose / 存档结构语义；saveVersion=48 永不变；applyDefaults 兜底链不变
2. **移动端永久禁止**：不新增、不恢复任何移动端适配内容
3. **同伴/队伍系统禁止**：工程 P 仅为单线剧情角色，不进入队伍界面、不改变战斗与行动系统
4. **设定一致**：新内容与 8 势力/五主线/七锚/铁牌/神谕/金秤/晨天严格一致；涉及伏笔必须同步账本；不自创冲突设定
5. **备份**：每次改动前备份 `backup\<批>_<日期>\`（如 F1_20260911）；临时脚本跑完归档 `backup\scripts_archive\tmp\`
6. **旧档兼容**：新功能全部独立键/默认值；S 零新增存档结构字段
7. **节点只增不降**（4,869 基线）；文本不删句不改既有选项语义；V66 文风 + 30 治理词 + 中文引号成对 + 战斗四段式
8. **新增节点必须**：带 pace 字段、进蓝图 nodeIndex（尾部锚点法）、涉及设定同步账本
9. **每批验证链**：`node --check`（26 块）→ `elda text guard` → `_build_authority.py build` → Copy game_built.html → game.html → `elda chunks` → `elda sync`（四路字节一致）→ `elda budget --reason "<批> <理由>"` → `elda ci`（39 检查器）→ `smoke_test.py` → bu 桌面 1280 实测 → docs 文档 → git 提交
10. **出现偏差立即停止报告，不绕行不降级**

## 每批交付

- 改动清单（文件 + 行号 + 前后差异摘要）
- 验证链结果（elda ci 全绿输出、smoke PASS、bu 实测数据；涉及账本批含 `elda content causality` 核销结果）
- `docs\` 对应文档（如 `F1_失败专线骨架.md`，含扩充记录：扩了什么、为什么、如何验证）
- present_files 交付后继续下一批（不停等）

## 验收标准

- **F**：30-40 条失败专线全部可触发可闭环；失败后可见支线 ≥3 条；蒙羞身份状态感知文本生效；失败回响 ≥15 处
- **E**：回响账本 ≥100 项；回响场景 ≥40 处全部触发正确；回响不改变判定与选项集
- **C**：聚合枢纽 10-15 个全部落地；两条不同路径到达同一枢纽正文不同、判定一致（bu 实测）
- **S**：n9 钩子注入正常；14 声音 300-400 条插话全量生效；声音冲突与专属选项实测；开关可关、关闭后与 v93 逐字节一致
- **T**：心念殿 20-30 概念全部可获取可内化；内化期 buff/debuff 正确；内化后专属内容 ≥20 处；立场轴联动实测
- **L**：线索碎片 ≥30 全部可接触；3 拼合枢纽按进度给不同正文；信息盲区对话差异实测
- **P**：搭档信任维度生效；20+ 互动节点全部可触发；"无声评判"四态正文（高/中/低/敌意）切换正确；不进入队伍系统
- **W**：事件池 176 → ≥300 全部微叙事化；阶段专属事件按状态正确门控；天赋协同隐藏链 ≥3 条
- **全程**：节点 ≥5,600；文本增量 ≥40 万字；saveVersion=48 不变、旧档兼容、四路字节一致、elda ci 39 检查器全绿、smoke PASS、bu 实测无回归
