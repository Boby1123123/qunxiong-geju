# I1-1 分段阅读渲染 — 实施记录

- 日期：2026-09-09
- 口令：《群雄割据 第0层文本基底整改 + 第1层叙事沉浸实施》批 I1-1
- 状态：✅ 完成，elda ci 全绿（可发布）

## 一、改动清单（文件 + 行号 + 前后差异）

### src\script_03.js（4 处）
| # | 位置 | 改动 | 差异摘要 |
|---|------|------|---------|
| A | applyDefaults（约 L42） | 加 `/t11inj:defaults/` 兜底块 | `if(!s.settings) s.settings={}; if(s.settings.pagedReading===undefined) s.settings.pagedReading=true;` —— 旧档兼容：新开关默认开启 |
| B | shouldPaginate（约 L2974） | 加 `/t11inj:switch/` 开关判断 | `try{ if(S && S.settings && S.settings.pagedReading===false) return false; }catch(e){}` —— 关闭时字符串型/长节点整段渲染，零回归 |
| C1 | renderPage 继续按钮分支（约 L3048） | 加 `/t11inj:keys/` 监听绑定 | 继续按钮创建时：story 元素绑定一次性 click（`__v45ClickH` 防重）；document 绑定 keydown（Space/Enter 且焦点不在 INPUT/TEXTAREA/SELECT 时 `preventDefault()+v45_continue()`） |
| C2 | renderPage 读完分支（约 L3031） | 加 `/t11inj:unbind/` 解绑 | `end>=ctx.txt.length` 时移除 keydown 与 story click 监听，防泄漏 |

### src\script_04.js（3 处）
| # | 位置 | 改动 | 差异摘要 |
|---|------|------|---------|
| D | v34_renderSettings（生效版 L9653 后） | 加设置行 | `v34_settingRow('分段阅读', '长剧情逐段显示，点击正文或 Space/Enter 继续', 'pagedReading', 'checkbox')`（注：文件含两版同名函数，仅改 L9646 生效版，L8676 旧版不动） |
| E | v34_settingRow 取值链（L9686） | 加 S.settings 兜底取值 | `... : (S&&S.settings&&S.settings[key]!==undefined) ? S.settings[key] : false` |
| F | v34_toggleSetting（L9709） | 加分支 | `else if(key==='pagedReading'){ S.settings.pagedReading=!!value; }` |

### src\script_03.js（建号兜底，第 5 处）
| # | 位置 | 改动 | 差异摘要 |
|---|------|------|---------|
| G | btn-start onclick 建号生效段（L3752 前） | 加 `/t11inj:defaults-build/` | 建号完成时补 `S.settings.pagedReading` 默认 true（实测发现建号不走 applyDefaults，S.settings 仅 `{transition:true}`，导致开关 UI 与行为不一致；已修复） |

## 二、验证链结果

| 项 | 结果 |
|----|------|
| node --check（26 块，elda ci 内） | ✅ PASS |
| 权威源幂等 verify | ✅ PASS |
| 四路字节一致 | ✅ game=game_check=6,075,117B；chunked=index=3,440,261B |
| elda full 18 检查器 | ✅ PASS（含文本质量、死链、结构健康） |
| 性能预算门 6 项 | ✅ PASS（budget.json 基线已更新：baseline.game_html 6,074,958→6,075,117B，reason「I1-1 分段阅读渲染+建号兜底完成」） |
| smoke_test.py | ✅ PASS（30 步推进、5 面板、存读档、结局可达） |
| bu 桌面 1280 实测 | ✅ 见下 |

### bu 实测数据（file:// 单文件 + 1280 视口）
1. 建号（普路托斯·男·人类·北境人·自由城邦·魔法师·天才·狩猎·探寻真相）→ 进入「交汇城·市井」长节点（10 段）✅
2. 分页渲染：仅显示前 3 段 + `▸ 继续阅读…` 按钮，`__v45ctx={page:1,total:10}` ✅
3. 点击继续按钮 → page 2 ✅
4. 点击正文（story）→ page 3 ✅
5. 键盘 Space（js 派发 keydown{key:' '}）→ 页推进 ✅（bu.press_key("Space") 键名差异属 bu 层，浏览器真实按键 e.key=' ' 正常触发）
6. 读完最后页 → 选项出现（3 选项）、v45-cg 消失、监听解绑 ✅
7. 设置面板：'分段阅读' 行存在；建号后 checked=true ✅
8. 开关关闭 → `v45_shouldPaginate(10段节点)=false`（整段渲染零回归）→ 重新开启 ✅

## 三、扩充记录

| 扩充项 | 为什么 | 如何验证 |
|--------|--------|---------|
| 设置面板「分段阅读」开关（S.settings 独立键） | 口令要求提供开关；独立键避免污染既有存档字段 | bu 实测开关开/关两态；旧档 applyDefaults/建号兜底均默认 true |
| story 点击 + Space/Enter 双继续入口 | 口令要求「点击正文/Space/Enter 继续」；与沉浸模式（I1-4）的 Space 推进语义统一 | bu 三种方式各推进一页；读完解绑无泄漏 |
| 建号完成兜底 | 实测发现建号路径不经过 applyDefaults，开关 UI 显示 false 与行为不符 | 重建后 bu 复测：S.settings.pagedReading=true、开关 checked=true |

## 四、红线对照

- 判定公式 / writeNext 核心语义 / choose / 存档结构语义：**零触碰**（writeNext 只读 shouldPaginate/renderPage 的既有返回路径）
- saveVersion=48 不变；applyDefaults 兜底链不变（新增 pagedReading 属追加默认值）
- 旧档兼容：S.settings 缺失时兜底 true；节点数 3,789 不降（ci 节点下限 PASS）
- 字符串型节点零回归：开关关闭时 shouldPaginate 直接 return false 走原渲染路径

## 五、备份与归档

- 备份：`backup\I1-1_20260909\script_03.js`、`script_04.js`（改动前快照）
- 临时脚本：`_i11_*.py` 已归档至 `backup\scripts_archive\tmp\I1-1\`
