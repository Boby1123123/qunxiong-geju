# M4 · 卷 B 同学第二幕 + 学院事件池

> 批次：M4｜日期：2026-09-10｜口令：《七锚之约》第 4/9 批
> 前置：M3（git 3703a88）｜后续：M5 锚1~4（未开工）

## 一、批定义（口令原文摘录）

- 新文件 `src\data_nodes\dn_alumni.js`；好感 ≥60 解锁（未达标显示"尚未收到来信"占位，达标后出现）
- 8 人链：rock / kain / alice / ata / loka / cecy / mori / elena 各 1~6；卷 A 救老铁后 alumni_tie_1~4
- 事件 10 则（毕业季风暴/百年校庆/校友回访/禁书区再响/费尔曼离职/图书馆火灾/恩师病危/北境征兵令/故乡来信/旧礼堂重建）
- 支线 2（导师传承 grad_mentor、五人小队重聚 alumni_squad）

## 二、改动清单

| 文件 | 改动 | 差异摘要 |
|---|---|---|
| `src\data_nodes\dn_alumni.js`（新） | 25 节点 | alumni_hub（15 选项入口）+ rock/kain/alice/ata 四链各 6 节点；文本三形态（default + ifRelation yes/no） |
| `src\data_nodes\dn_alumni2.js`（新） | 36 节点 | loka/cecy/mori/elena 四链各 6 + alumni_tie_1~4 + grad_mentor_1~4 + alumni_squad_1~4；全部对象式、带 pace |
| `src\data_nodes\dn_story_blueprint.js` | +61 项 | 蓝图 nodeIndex 扁平属性区尾部注入（`"grad_home_8"` 末项前，尾补逗号、末项无逗号），vol_academy/arc_academy |
| `src\data_nodes\dn_causality.js` | +10 项 | LEDGER 尾部注入 led_alumni_01~10（全部 status:open，指向 M5/M6 锚线核销）；WORDS 保持纯净 20 词 |
| `src\script_01.js` | +10 则 | EVENT_POOL_EXT 尾部注入（day 42/87/108/143/167/193/218/242/267/292，与五主线 60/120/200/250/280 错开 ≥12 天）；事件池 121→131 |
| `src\data_nodes\dn_graduate.js` | +4 选项 | grad_stay_8/army_8/roam_8/home_8 各追加 `{t:"（顺路去驿站，看看有没有信）",go:"alumni_hub"}` |
| `game.html`/`game_built.html`/`game_check.html`/`game_chunked.html`/`index.html` | 重建 | 7,639,059B / 4,982,882B（四路一致） |

### 治理词修复（T0 铁律）
- M4 初稿引入 5 处"压低声音"（"低声"子串）、隐约×1、微微×1 → 全部替换为具体动作（"声音压得只剩火堆能听见"/"放轻了嗓门"/"凑近半步"/"声音发沉"/"浮着几道烟柱"/"掌心发烫"）
- 修复后 `elda text guard` 全绿：30 词全 ≤30、引号配对 2,968/2,968、连续标点 0

## 三、验证链结果

```
node --check（26 块 + data_nodes）     → 0 失败
elda text guard                       → 全绿（30 词≤30 / 引号成对 / 连续标点 0）
_build_authority.py build             → OK（19 script 块，4,743,747 字符）
elda chunks + sync                    → 四路一致 game=7,639,059B / chunked=4,982,882B
elda budget --reason                  → baseline 更新（node_total_min=4,285）
elda ci（24 检查器）                   → 全部通过（可发布）
smoke_test.py                         → PASS
bu 桌面实测（视口 967×883）            → 见下
```

### bu 实测记录（computer_use_tool plane="bu"，file:// game.html）

| 实测项 | 结果 |
|---|---|
| 重载后 N 节点注册 | alumni_hub/alumni_rock_1 等全部在运行时 N（nKeys 2,639） |
| 读档（第 1550 日存档，点"确 定"） | 通过，回 grad_choose |
| `v488_go('alumni_hub')` 渲染 | 「北境 · 驿站」标题卡 + 正文 + 15 选项（8 同学/老铁/导师/小队/4 去向出口）全部渲染 |
| ifRelation 变体 yes（rock=80） | 正文切换为狼尾毛段落（"草原人送狼毛，是把命交到对方手里一半"） |
| ifRelation 变体 no（kain=20） | 正文切换为短信（"信很短，像是军务间隙草草写的"） |
| grad_stay_8 驿站入口 | 选项含"（顺路去驿站，看看有没有信）" |
| 存档读写 | StorageKit.save(好感) → reload → load 恢复 npcRelations{rock:80,kain:20}，saveVersion=48 不变 |
| 截图留证 | alumni_hub 完整界面（顶栏+正文+选项+状态栏） |

> 注：产品浏览器窗口视口固定 967×883（resizeTo 不生效），768/375 移动档因用户此前已要求删除移动端适配，不再单独实测。

## 四、扩充记录（扩了什么 / 为什么 / 如何验证）

1. **hub 四去向出口**：口令未指定 hub 出口，为闭环加入"回学院续职/回营房报到/继续游历/继续回乡的路"四选项（go 回 grad_stay_8 等既有节点）——为什么：hub 需可退出且不破坏毕业去向语义；验证：v488_go 跳转 + 选项渲染实测。
2. **ifRelation 变体双分支**：8 同学链首节点均用 `text:{default, ifRelation:{npc,op:">=",val:60,yes,no}}` 三态——为什么：满足"好感 ≥60 解锁、未达标占位"的口令要求且零引擎改动（复用 CM-2 v91_resolveText）；验证：rock=80 出狼毛段落、kain=20 出短信，两分支实测。
3. **老铁链 alumni_tie_1~4**：口令"卷 A 救老铁后"——入口放 hub 第 9 选项，正文用 ifRelation tie≥60 区分"收古董的人打听铜钟"详情；为什么：与 M1/M2 第三哨铜钟/铁板线衔接；验证：变体渲染 + 蓝图/账本登记。
4. **账本 10 项 led_alumni_01~10**：全部 status:open、reap 指向 M5/M6 锚节点（anchor_tower/mine/oracle/chen/church/goldscale/warphase）——为什么：把 M4 埋设的 9 条线索 + 导师遗物线纳入 CM-3 机器账本；验证：elda content causality closed=89/open=7（M4 后），M5/M6 核销。
5. **事件文本互文**：alumni_home_call（洛克立功）呼应 rock 链、alumni_plague（东境疫病）呼应 loka 链、alumni_restrict/ferman/fire（禁书区/费尔曼/图书馆）呼应 alice 链与 I1 学院线；为什么：世界事件与区域节点互文原则；验证：day 错开 + ci 事件检查通过。

## 五、基线快照（M4 后，供 M5 对照）

- 节点 **4,335**（M3 4,274，+61）；事件池 **131**；账本 **96 项**（closed=89/open=7）；设定词 20/20
- 四路：game=game_check=**7,639,059B**；chunked=index=**4,982,882B**
- budget：node_total_min=4,285；elda ci 24 检查器全绿；smoke PASS
- saveVersion=48 不变；旧档兼容（新增全部独立键/flag/节点）
