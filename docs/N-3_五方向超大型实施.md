# N-3 五方向超大型实施口令（关系深化 / 动态世界 / 战后深渊 / 多周目 / 节奏手感）

依据：上轮方向推荐（五方向按优先级排序），用户指令"全部按照推荐的顺序给我口令，然后不中断的直接完整开跑"。

## 基线（2026-09-11，v92 八批后）

- 节点 4,699（单文件）/ 4,795（分片合并）；事件池 176；账本 167（closed=147 open=18）；设定词 20/20
- 四路字节：game=game_check=8,759,930B；chunked=index=5,989,362B
- budget：game 8,809,930 / chunked 6,089,362 / node_total_min 4,649；saveVersion=48
- ci 25 检查器全绿；smoke_test.py 20 路径 PASS
- git HEAD：016b7b2（八批大型更新已推送）

## 批次总表（15 批，批次间不停等，全部做完不缩水）

### 线一 · 关系系统深度化（REL-1 ~ REL-5）
- REL-1 送礼系统：GIFT_ITEMS_V93（8 件礼物×喜好表）+ 送礼节点链（选礼→送出→四档反应）+ 好感变化
- REL-2 邀约/同行：邀约节点链（去酒馆/散步/同行护送），同行时事件文本变体
- REL-3 跨城书信：写信/回信节点链 + S.letters 独立键（异地带信，收信文本呼应）
- REL-4 关系事件链：≥3 条多选事件链（吃醋/误会/背叛/和解），各 4-6 节点闭环
- REL-5 关系影响世界：好感≥60 NPC 在战争/终局节点的文本与选项变体（ifRelation）

### 线二 · 动态世界响应（WDR-1 ~ WDR-3）
- WDR-1 世界状态仪表盘：五维 0-100 可视化面板 + 玩家因果记录
- WDR-2 抉择长期后果：≥10 城市 hub 挂 ifFlag 状态变体（flag→城市可感知改变）
- WDR-3 玩家势力据点：据点系统（建据点/招人/被围攻/商路垄断），S.base 独立键

### 线三 · 战后 + 深渊内容包（AP-1 ~ AP-4）
- AP-1 战后世界章：postwar_* ≥20 节点（重建/新秩序/旧友去向/金秤后日谈）+ 事件 5
- AP-2 深渊裂隙：abyss_* ≥16 节点（seal 后深入/封印核心/裂隙抉择）+ 事件 4
- AP-3 神谕之地：oracle_land_* ≥12 节点（狼骨/灰鬃/神谕展开）+ 事件 3
- AP-4 旧版资产回收：backup\archived 挑 ≥8 设定/人物重写融入 + 清单

### 线四 · 多周目变奏（NG-1 ~ NG-3）
- NG-1 NG+ 异变：周目 2+ 事件文本变体 + NPC 记得你（配合记忆注入）
- NG-2 结局回收图鉴：结局列表 + 达成条件提示 + 隐藏结局解锁
- NG-3 挑战模式：S.challenge 独立键（铁人/限时/锁职业）

### 线五 · 节奏手感（FEEL-1 ~ FEEL-4）
- FEEL-1 当前目标引导：S.goal 独立键 + 顶栏/面板目标显示
- FEEL-2 修为可视化：修为条 + 突破演出升级
- FEEL-3 旅行系统：补给/天气影响决策（旅途选项）
- FEEL-4 死亡回溯：死亡后回溯选择/遗产

## 铁律（全程）

- 每次改动前备份 backup\<批>_<日期>\；临时脚本归档 backup\scripts_archive\tmp\
- 不触碰判定公式 / writeNext 核心语义 / choose / 存档结构语义；saveVersion=48 不变；applyDefaults 兜底链不变
- 旧档兼容：新功能全部独立键/新节点/新 flag；五主线 day 判定零改动
- 节点只增不降；文本不删句不改既有选项语义；V66 文风 + 30 治理词 + 中文引号成对；战斗四段式
- 新节点：带 pace 字段、进蓝图 nodeIndex（尾部锚点法）、涉及设定同步账本；新事件 day 与五主线错开、EVENT_POOL_EXT 数组末项逗号
- 不做移动端；不新增移动端内容

## 每批验证链

node --check（26 块）→ _build_authority.py build → Copy game_built.html → game.html → elda chunks → elda sync（四路一致）→ elda budget --reason "<批> <理由>" → elda ci（25 检查器）→ smoke_test.py → bu 桌面 1280 实测（分批轮换覆盖）→ docs 文档

## 每批交付

改动清单（文件+行号+前后差异）→ 验证链结果 → docs\ 对应文档（含扩充记录）→ 批次间不停等

## 验收标准

- REL：送礼/邀约/书信/事件链/世界影响 五系统全部可触发；≥3 条关系事件链闭环；好感变化正确
- WDR：仪表盘可视化正确；≥10 hub 状态变体生效；据点系统完整闭环
- AP：postwar/abyss/oracle 三包全部可玩；事件 ≥12 新则；回收 ≥8 资产合规融入
- NG：NG+ 变体实测生效；结局图鉴显示正确；挑战模式开关有效
- FEEL：目标引导显示正确；修为条/突破演出升级；旅行补给判定生效；死亡回溯可用
- 全程：saveVersion=48 不变、旧档兼容、四路字节一致、elda ci 25 检查器全绿、smoke PASS、bu 实测无回归
