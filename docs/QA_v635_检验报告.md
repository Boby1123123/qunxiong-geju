# QA_v635_检验报告（移片扩容 + 网络化地基）

- 生成时间：2026-09-08 13:49
- 基线：backup\game_v635_before.html（MD5 55E9A32ABA0F99E0B3F87BA2CA59331D）

## 一、方向一：移片扩容（完成）

| 批 | 片 | 前缀组 | 节点 | 验证 |
|---|---|---|---|---|
| 1 | academy | academy | 269 | full 全绿（9 片 1188）|
| 2 | city | city, cityev | 161 | full 全绿（10 片 1349）|
| 3 | npc | npc, classmate | 155 | full 全绿（11 片 1504）|

- 主文件：6,673,758 → 5,556,624 字节（**净降 1.12MB，超 0.8MB 目标**）
- 总节点 3,409 不变；死链 0；缺失 0；多余 0
- 多前缀支持：loader owner 按 prefixes 数组归属（cityev→city / classmate→npc），c_chunks 同步升级

## 二、方向二：网络化地基（完成）

| 项 | 结果 |
|---|---|
| elda serve | ✅ 新增子命令，冒烟通过：http://127.0.0.1:8123/index.html HTTP 200（2,964,982 字节）+ chunks/v62_academy.js HTTP 200（475,465 字节）|
| 存储后端 | ✅ StorageKit 三层存储已完整实现（localStorage 压缩 + IndexedDB 异步双写 + 导出/导入灾备），本轮确认并文档化，不重写 |
| 云存档槽位 | ✅ v63_cloud 注释槽位已注入（game.html /v635inj:store/），方案文档 docs\云存档方案_v635.md |
| 双轨 | ✅ file:// 与 http:// 并存；README 验证链加入可选 elda serve 回归 |

## 三、验证结果汇总

| 验证项 | 结果 |
|---|---|
| elda full 十一项 | ✅ 全部通过（FAIL 0 / 分片 11 片 1504 节点死链复核 0）|
| elda chunks | ✅ 合并 3409 节点缺失 0 多余 0 死链 0 占位 1 |
| elda sync | ✅ game=check 5,556,624 字节；chunked=index 2,964,982 字节 |
| node 仿真 | ✅ 全部通过（11 片 manifest / owner 多前缀 / 5 片合并 1199 节点 / mark / writeNext 包装 / 按需加载）|

## 四、风险与遗留

1. **浏览器回归未执行**（bu 会话超时）：手动清单——file:// 打开 game.html：学院线走 3 节点（academy 片首载）→ 城市线 2 节点 → 偶遇 NPC 1 节点 → 0 console 新错误；可选 elda serve --open 走 http 同路径
2. StorageKit 灾备读取未接入 loadGame（loadIDB API 在但读档仍 LS 优先）——设计如此（LS 为主 + IDB 灾备），不改核心流程
3. 后续移片候选见 docs\移片路线图_v635.md（sub/h_/ending 移前须人工确认）
4. 云存档待用户提供 Supabase URL + anonKey 后按 docs\云存档方案_v635.md 启用
