# TQ34 · 第三十四批 · 薄节点扩写（dn_faction2.js 四势力线）

- 批次：b34（TQ 文本本体革命·薄节点扩写流水线）
- 日期：2026-09-12
- 对象：`src\data_nodes\dn_faction2.js`（M9 卷 D 后四势力线：东境承天 faction_east_1~13 / 兽人诸部 faction_orc_1~13 / 矮人山国 faction_dwarf_1~13 / 精灵林邦 faction_elf_1~13，各 13 节点，tag:"main"）
- 规模：53 个 <300 字薄节点全量扩写至 400+（tq34.py 52 节点 + tq34b.py 余 29 节点补第三段）
- 全局薄池：720 → 667

## 扩了什么

### 选区依据
`tools\scripts_tmp\thin_pool.json` 扫描（只扫 dn_*.js）：dn_faction2.js 薄节点 53 个，覆盖四势力全部主线节点，是当时薄池第一大文件。

### 扩写手法（沿用 b32/b33 实证）
- 统一**末段前插入**模式：每节点在原 text 数组最后一段之前插入 2 段（tq34.py），扩后仍 <300 字的 29 节点补第 3 段（tq34b.py）。末段"离开句/收束句"（如"你最后回望一眼承天城，转身穿过街口，往下一程赶路。"）保留在末尾，收尾节奏不被破坏。
- 插入逻辑：seg_starts 定位末段引号起点 → 在末段前 `,新段1,新段2,` 拼接（**首版缺末段前逗号致 SyntaxError，git checkout 恢复后修正重跑**，见下）。
- 新增段 = 环境白描（2 感官）+ 内心收束两段一组；与各势力既有伏笔互文：
  - 东境：晨天故都旧档、秦守将、贡院考题"秤在手里民在心里"、水淹七百里密计、逃难人流
  - 兽人：狼群南迁、老萨满兽骨、铁门关城下、守秤人、阿岩
  - 矮人：三百年的门、七枚黑铁锚刻痕、铁王座大锤、老铁匠掌纹锤柄
  - 精灵：世界树搏动、晨星之泪裂纹、巡林长、哨箭、封印松动

### 质量约束执行
- 30 治理词全程规避，三处漏网（tq34.py：east_4"仿佛"、orc_12"缓缓"、dwarf_5"轻轻"）在 ci 防回潮 FAIL 后逐一改写为"像是/向北移去/叩着"，重跑全链。
- 中文引号“”成对、无连续标点、无英文引号包中文；不改任何选项/判定/go/effects；节点数零变化（纯文本追加）。

## 为什么

用户持续要求"节点故事衔接与文本长度深度达到优秀文游标准"（TQ 主线）。四势力线是 M9 卷 D 的运行时核心（阵营加入/晋升/抉择/背叛），原文本多为 130-264 字的干骨架，扩写后每个节点具备可沉浸的环境、在场人物与内心权衡，符合 V66 四段式模板（环境白描→人物细节→事件→内心收束）。

## 验证链结果

| 环节 | 结果 |
|---|---|
| node --check（26 块） | PASS（含 dn_faction2.js 语法） |
| elda text guard | PASS（30 词全 ≤30 / 引号左=右=7774 / 连续标点 0） |
| elda backup | snap_20260912_132528_TQ34-faction2扩写.zip（39,920 KB，保留 12 份轮换） |
| _build_authority.py build | OK：19 script 块 → game_built.html（10,460,462 字符） |
| Copy → game.html | 已同步 |
| elda chunks | 43 JS 全 PASS；合并节点 5,732；死链 0 |
| elda sync | 四路一致：game=game_check=18,905,471B / chunked=index=7,837,681B |
| elda budget | game_html_bytes_max → 18,955,471（baseline 同步 18,905,471，理由真实） |
| elda ci | **41 检查器全绿**（V66 风格锁防回潮 PASS、节点覆盖 5,732/5,732、性能 18.21s、预算 6 项全 PASS） |
| smoke_test.py | 见下 |
| 薄池复查 | dn_faction2.js 薄节点 53 → 0；全局 720 → 667 |

### 过程记录（踩坑与修正）
1. **末段前插入缺逗号**：首版 `add = ',' + join(segs)` 在末段引号前插入，新段与末段间无逗号 → SyntaxError。git checkout 恢复后改为 `',' + join + ','`，重跑成功。
2. **PowerShell 内联 python 转义**：修复脚本时用内联 `-c` 带复杂引号 → PS 解析失败，整条命令未执行（checkout 未生效，文件仍为污染版）。改用 Edit 工具修脚本后再恢复文件重跑。
3. **V66 风格锁防回潮 FAIL**：轻轻 35→38 / 缓缓 20→23 / 仿佛 23→26（我的 3 处新词）→ 逐处改写 → 重建全链 → 全绿。

## 如何验证

- `python -X utf8 tools\scripts_tmp\tq_scan_pool.py` → thin_pool.json：faction2 计数 0。
- `python -X utf8 tools\elda\elda.py ci` → 41 检查器全绿（含 V66 风格锁、四路一致、预算门）。
- `python -X utf8 smoke_test.py` → PASS（建号→推进→面板→存读档→结局）。
- 浏览器实测（bu 桌面 1280）：建号后推进至卷 D 四势力任一势力线，节点正文均已扩至 400+，末段离开句仍在末尾，选项与判定不变。

## 遗留与后续

- 全局薄池剩 667 个 <300 字节点；下一自然选区：dn_fs_join.js(47) / dn_failpath.js(45) / dn_u8.js(42) / dn_faction.js(40)。
- 本批未新增账本项（扩写只引用既有伏笔，不埋新坑）；未新增事件；未触碰引擎/存档/判定。
