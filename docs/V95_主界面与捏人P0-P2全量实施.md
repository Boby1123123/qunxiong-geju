# V95 · 主界面与捏人 P0/P1/P2 全量实施口令

> 依据：本口令为唯一内容蓝图。基于 V94 现状（羊皮纸主界面四按钮 + 五步捏人向导 + 预览卡），把「入口顺手 + 创角与剧情联动 + 世界感 + 进阶体验」三层（P0/P1/P2）全部深度落地。
> 项目根：`D:\1pao tuan\群雄割据`　基线：节点 5,636 / elda ci 41 检查器 / saveVersion=48 / 四路 game=18,986,342B、chunked=7,918,329B

## 引擎锚点（改动前复核，以实际为准）

- 主界面 = `src\script_03.js:7033` v94_titleScreen（ts-new/ts-load/ts-ach/ts-codex 四按钮，:7046-7051；foot 存档摘要 :7052；ts-new 有档确认覆盖 :7056-7060；ts-load 单槽读档 :7075-7092）
- 捏人向导 = creationHTML :6758（五步：壹名讳血脉/贰出身/叁职业/肆天资爱好/伍理想属性启程）；showCreation :5336（openModal 挂载）；bindCreation v94 版 :6826（结算链 btn-start :6888，**零改动**）；v94_showStep :6964（步骤切换/标签 done/prev/next/hint）；v94_creationPreview :6989（预览卡"此行将引向"）；v94_creationReady :6987
- 捏人 CSS = `src\gap_00.html`：V94 title 块 :729-782、旧 #title-screen :1205、捏人向导全屏 flex 块 :1228（#creation-shell fixed 全屏 / .c-body flex:1 滚动 / .c-nav flex:0 0 auto 物理固定底部）
- 数据源 = window.ORIGIN_PROFILE（五维映射，:4816 _build）/ JOBS / HOMELANDS / IDEALS / HOBBIES / TALENTS / RACES / SUBRACES / BACKGROUNDS_FULL
- 存档 = S.slotId（applyDefaults :5327 兜底 "slot1"）；saveGame :5322 → StorageKit.save(S)（按 slotId）；v94 sniffRaw 读 `RULESET_ID+"-save"` 固定键（槽位化需复核 StorageKit 键名）
- 成就 = elda_achievements 独立键（:437），v34_openAchievements 成就册面板（含 unlockedEndings/achievements/stats）；解锁判定 :504 附近（**零改动**）
- 设定册 = v94_settingPanel :7094（世界大势 LOREBOOK constant / 九域 REGIONS / 五主线 WORLD_EVENTS）
- 世界书 = LOREBOOK（UPG-01，constant + triggers）；账本 = dn_causality.js（含 world 字段）
- 音效 = WebAudio 合成（acInit :955，判定音效注入 :1089-1091）——捏人点击音可复用
- 验证链 = node --check（26 块）→ `_build_authority.py build` → Copy game_built.html → game.html → `elda chunks` → `elda sync`（四路一致）→ `elda budget --reason "<批> <理由>"` → `elda ci`（41 检查器）→ `smoke_test.py` → bu 桌面 1280/768 两档实测 → docs 文档 → git 提交推送（push 被拒则 pull --rebase 重推）

## 执行范围（三层十一批；批次间不停等，全部做完，内容不缩水）

### 批 MP-1 · P0 继续旅程主入口
- 主界面新增"继续旅程"置顶按钮（ts-continue，primary 强化样式，置于 ts-new 上方）：有存档（sniff 命中）且无未终结存档时显示；无档隐藏
- 点击 = 一键续玩：直接走 v94_loadSave 逻辑（跳过确认框）；与 ts-load 区别：ts-continue 一键、ts-load 弹槽位面板（批 MP-2 后）
- ts-new 保持"有档确认覆盖"逻辑不动；foot 存档摘要增强（名字/天数/curCity/职业）
- 纯展示层 + 入口路由，判定/存档零改动

### 批 MP-2 · P0 存档槽位系统
- 主界面 ts-load → 槽位面板（3 槽：槽位一/二/三）：每槽显示 名字/天数/地点/最后保存时间（空槽显示"—— 空 ——"），点击即读档；面板关闭按钮
- ts-new 建号结算后进入"选择存档槽位"确认（3 槽选择 + 覆盖提示：目标槽非空时确认覆盖；全满默认槽位一）
- 机制：S.slotId 已存在（1/2/3）；**复核 StorageKit 键名**（elda-qunxiong-v3-save 是否按 slotId 区分），必要时槽位键 `RULESET_ID+"-save-"+slot`；v94 sniffRaw 改按槽位读；saveGame 写入 S.slotId 槽（saveGame :5322 不动，仅确保 S.slotId 正确传递）
- 旧档兼容：原 slot1 存档直接出现在槽位一；槽位面板读取失败回退单槽显示
- 不触碰存档结构语义 / saveVersion=48

### 批 MP-3 · P0 出身×职业联动
- 新数据文件 `src\data_nodes\dn_fit.js`：`window.FIT_MATRIX`——9 出身 × 12 职业适配矩阵（每格 1-3 星 + 一句"适配理由"），按世界观/人物设定编写（如自由城邦→游侠★★★/商人★★★/盗贼★★★；北境→战士★★★/猎人★★★；教会辖区→牧师★★★；精灵→法师/吟游诗人★★★ 等），不改变 JOBS/HOMELANDS 任何判定数据
- 贰出身页：每张出身卡加"适配职业"角标（top 3 职业名，读 FIT_MATRIX）
- 叁职业页：按已选出身给职业卡加适配标记（★★★ 推荐边框 / ★★ / ★ 冷门提示），未选出身时提示"先选出身以查看适配"
- 全部纯展示层（CSS 类 + 角标），选项/判定/go 零改动

### 批 MP-4 · P0 确认终审页（第伍步增强）
- 第伍步顶部新增"终审卡"（v94-final-check）：汇总 名讳/性别/种族/亚种/出身/谱系/职业/天赋/爱好/理想 十项，每项一行（值 + 一句后果/预告），点击任一行 → 跳对应步骤（v94_showStep）
- 终审卡下保持属性分配 + d100 说明 + btn-start（文案改"确认并踏入4037年"）
- bindCreation 结算链（:6888 btn-start）零改动；v94_showStep 第伍步 hint 更新为"核对终审卡；点击任意条目可返回修改。确认后踏入4037年。"

### 批 MP-5 · P1 世界导入屏（4 屏幻灯片）
- 新增 `v94_introOverlay`（全屏层，复用羊皮纸视觉 + title 动效）：第1屏 世界大势（诸神远去/七锚/金秤守墓/深渊低语，取 LOREBOOK constant）、第2屏 九域与势力（REGIONS 动态渲染九域名+一句 desc）、第3屏 时代洪流（WORLD_EVENTS 五主线 day+名）、第4屏 你的开局（灰港渡船 + 五维选择的意义 + "开始捏人"按钮）
- 交互：前进/后退/进度点（第X/4屏）/「跳过 >>」；老玩家判定（elda_achievements 有记录 或 检测到存档）→ 自动显示跳过高亮，仍可完整看
- 接入：ts-new（无档路径）→ v94_introOverlay → 完成后 v94_startNew()（showCreation）；ts-continue/ts-load 不经过导入屏
- 不改变 newGame/emptyState 后任何状态；全部独立函数独立 DOM

### 批 MP-6 · P1 成就册增强
- v34_openAchievements 面板增强（不改解锁逻辑 :504）：
  - 未解锁成就显示占位卡（名字"？？？"+ 分类提示词）
  - 分类页签：剧情/战斗/成长/结局/隐藏（defs 结构复核后按现有字段归类，缺字段给默认）
  - 总览进度条（已解锁 X / 总 Y）
  - 已解锁项显示解锁时间（Date.now() 已存，格式化显示）
- 数据源 elda_achievements 零改动；面板层纯新增

### 批 MP-7 · P1 设定册增强（词条库）
- v94_settingPanel 扩为完整词条库（保留现有三块），新增分类：
  - 人物志：从 LOREBOOK triggers 登记人物（金秤家族/秦·长风/灰鬃/林歌/老铁/李管事/学院导师 等）+ 账本 world 字段人物类 → 词条卡（名字/一句话身份）
  - 关键地点：七锚/晨天故都/铁门关/第三哨/学院/沙漠五城/西境三城（REGIONS + 账本地点类）
  - 术语与神器：铁牌/神谕/腐光/金秤信物/七锚（LOREBOOK + 账本）
  - 势力矩阵：8 势力关系一句话（读账本/REGIONS desc）
  - 搜索框（按词条标题/正文过滤，即时）
- 全部动态生成（LOREBOOK/REGIONS/WORLD_EVENTS/账本为源），不硬编码正文；S 无关（主界面可用）

### 批 MP-8 · P2 捏人预览卡增强
- v94-preview 增强（保留"此行将引向"摘要）：
  - 实时属性总览行：当前六属性值 + 种族/亚种加成 + 天赋加成 + 剩余点数（读 S.attrs + RACES/SUBRACES/TALENTS 加成，每步切换即时刷新）
  - 五维摘要补齐：已选五项（出身/职业/理想/爱好/天资）+ 未选占位"尚未选择"
  - 节奏适配：preview 侧栏在 883 视口下保持 sticky 顶部、内容压缩（字号 12px 级别）
- v94_creationPreview :6989 扩展（只读 S，零写入）

### 批 MP-9 · P2 名字随机器 + 属性快速模板
- 新数据文件 `src\data_nodes\dn_namegen.js`：`window.NAME_POOL`——7 种族 × 12 个符合西幻命名（人类/精灵/矮人/兽人/半身人/精灵血/龙裔风格），含种族风味注释
- 壹步姓名输入框旁新增「🎲 随机」按钮：按已选种族从 NAME_POOL 抽 3 候选（下拉/横排小卡），点击候选填充输入框（不覆盖手动输入除非点击）
- 伍步属性区新增 5 个一键模板按钮：均衡型/力量型/敏捷型/法师型/魅力型（各 6 维分配模板，20-80 范围内，与天赋加成后的剩余点数联动——按当前 pool 重算分配）
- 纯前端数据层，属性校验/结算链零改动

### 批 MP-10 · P2 捏人音效反馈
- 复用 WebAudio（acInit :955）新增轻量合成音：步骤切换（纸页翻动感：短噪声包络）、选择卡片（木质短促音）、开始旅程（低沉号角感：两个叠加振荡器）
- 新函数 `v94_sfx(type)`（放 script_03.js v94 区），hook 进 v94_showStep 切换 / sel-card 点击（bindCreation 内统一绑定，不逐卡改）/ btn-start
- 音量低（gain 0.05-0.08）；设置面板"音效"开关（saveSetting("sfx")）生效时播放，off 时静默；无 AudioContext 时静默降级
- 不动既有判定音效注入（:1089-1091）

### 批 MP-11 · P2 出生微叙事过渡
- 结算衔接：btn-start（结算链末尾）后、正文渲染前，插入 1 段 200-400 字"出生微叙事"全屏卡（v94-birth-card）：按 出身+亚种+职业 组合从 ORIGIN_PROFILE 数据拼装（每出身 1 段开篇白描 + 亚种一句 + 职业一句展望），纯展示（"……你踏上灰港的渡船"收尾），点击"启程"关闭并进入正文
- 实现：v94_creationReady / btn-start 结算完成后先显示 birth-card，关闭后走既有正文渲染路径（newGame 已执行的状态不回滚，仅延迟正文显示）——**需复核结算链时序**（btn-start :6888 做了什么、正文入口在哪），确保插入点不改变 S 状态
- 文本 V66 文风 + 治理词 + 中文引号成对

## 铁律（全程）

1. 不触碰判定公式 / writeNext 核心语义 / choose / 存档结构语义；saveVersion=48 永不变；applyDefaults 兜底链不变
2. 移动端永久禁止（不新增任何移动端适配）；不做方向六；不做同伴/队伍系统
3. 每次改动前备份 `backup\<批>_<日期>\`（如 MP1_20260912）；临时脚本跑完归档 `backup\scripts_archive\tmp\`
4. 旧档兼容：新功能全部独立键/独立 DOM/独立函数；S 新增字段仅允许展示层临时量（不入存档）
5. 节点只增不降（5,636 基线）；文本不删句不改选项语义；V66 文风 + 30 治理词 + 中文引号成对
6. 主界面/捏人改造全部为展示层与入口路由，任何判定/结算/存档读写路径零改动（MP-2 槽位键复核除外——复核结果文档记录）
7. 每批验证链：node --check → elda text guard → build → copy → chunks → sync（四路一致）→ budget --reason → elda ci（41 检查器）→ smoke_test.py → bu 桌面实测 → docs 文档 → git 提交推送
8. 出现偏差立即停止报告，不绕行不降级

## 每批交付

- 改动清单（文件 + 行号 + 前后差异摘要）
- 验证链结果（elda ci 全绿输出、smoke PASS、bu 实测数据——MP-2 含槽位读写实测、MP-5 含导入屏四屏截图级数据）
- `docs\` 对应文档（MP-1_继续旅程.md … 或合并 V95 段，含扩充记录：扩了什么、为什么、如何验证）
- present_files 交付后继续下一批（不停等）

## 验收标准

- MP-1：有档无会话时"继续旅程"置顶显示，点击一键进游戏零确认框；无档不显示；ts-new 覆盖确认逻辑不变
- MP-2：3 槽位读写/覆盖/空槽正确；槽位摘要（名字/天数/地点/时间）显示正确；旧 slot1 档出现在槽位一；读档失败回退单槽
- MP-3：FIT_MATRIX 9×12 生效；出身页适配角标、职业页推荐标记正确；未选出身有引导提示；判定零改动
- MP-4：终审卡汇总十项；点击条目跳对应步；btn-start 结算链字节级零改动
- MP-5：4 屏导入可前进/后退/跳过；老玩家自动高亮跳过；newGame 后状态与基线一致
- MP-6：成就分类/占位/进度条/解锁时间；解锁逻辑零改动
- MP-7：设定册 5+ 分类全部动态生成；搜索过滤可用；S 无关
- MP-8：预览卡实时属性总览 + 五维摘要；每步切换即时刷新；侧栏 883 视口可用
- MP-9：7 种族随机名 3 候选可填充；5 属性模板一键分配正确（20-80 内、与 pool 联动）
- MP-10：捏人三步音效 + 开始音；sfx 开关生效；既有判定音效零改动
- MP-11：出生微叙事按出身/亚种/职业变体正确；结算后状态与基线一致（正文渲染时序复核通过）
- 全程：saveVersion=48 不变、旧档兼容、四路字节一致、elda ci 41 检查器全绿、smoke PASS、bu 两档实测无回归、git 已推送
