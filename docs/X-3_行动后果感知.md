# X-3 行动后即时"后果感知"文本（V66 白描）

> 批次：X-3 ｜ 日期：2026-09-11 ｜ 状态：已完成并验证
> 前置：ACT-2 v93_refreshAfterAct（src\script_04.js）

## 一、目标

行动判定结算后，正文不重绘的前提下，让玩家"感知"到行动的物理后果——伤口、钱袋、精神损耗。落在行动日志区（feel 条目），不污染正文流。

## 二、触发条件（对比上次行动结算 `__v93LastActEnd`）

| 差值 | 触发 | 模板数 |
|---|---|---|
| hp ≤ -3 | hurt（伤口白描） | 3 |
| gold ≤ -2 | poor（钱袋变轻） | 3 |
| gold ≥ +3 | rich（进账） | 3 |
| san ≤ -5 | san（精神损耗） | 3 |

模板为 V66 古典白描（如 hurt 例："你低头看了一眼，血已经把里衣洇出一片暗色。"），零治理词、禁 AI 高频表达。

## 三、改动清单

文件：`src\script_04.js`（act IIFE 内）

| 位置 | 改动 |
|---|---|
| act IIFE | 新增 `window.__v93AfterFeel`（四类模板对象）与 `window.__v93LastActEnd` |
| `v93_refreshAfterAct` 开头 | /x3inj:afterfeel/ 块：读 S.hp/gold/san 与 LastActEnd 对比 → pushLog(feel)；末尾更新 LastActEnd |

补丁工具：`tools\scripts_tmp\x23_patch.py`（X-2/X-3 同补丁，已归档）。

## 四、验证（bu 实测）

模拟：S.hp=50/gold=100/san=30 → 设置 __v93LastActEnd 同值 → S.hp=45/gold=96/san=24 → v93_refreshAfterAct()

结果（elda-actlog-v2 持久化条目）：
```
0:feel | 你低头看了一眼，血已经把里衣洇出一片暗色。
1:warn | ── 世界事件已至：『净化令』，旅途或相应区域可循迹触发。
```
- feel 触发正确（hp -5 → hurt）✅
- 世界事件感知不丢失（warn 条目照常，事件文本增量出现）✅
- 日志与正文隔离，正文段落数零增长 ✅

## 五、扩充记录

| 扩充 | 为什么 | 如何验证 |
|---|---|---|
| 后果感知四类 12 句 V66 白描 | 判定后有"身体/钱包的实感" | 四类差值分别触发对应模板（实测 hurt） |
| 与事件感知共存 | 行动刷新不丢世界事件 | refresh 后 warn 条目仍在 |
| 只读 S、不入存档 | 红线（存档结构零改动） | 存档字段无新增，saveVersion=48 不变 |
