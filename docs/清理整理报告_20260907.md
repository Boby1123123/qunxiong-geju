# 清理整理报告

> 日期：2026-09-07 · 目录：`D:\1pao tuan\群雄割据`
> 原则：**不删除任何核心交付物，所有"删除"均以移动到 `backup\cleanup_20260907\` 实现（100%可回退）**

## 一、清理前后对比

| 指标 | 清理前 | 清理后 |
|---|---|---|
| 根目录文件数 | ~190（脚本/产物混堆） | **79**（核心+验证链+数据卷） |
| 脚本组织 | 全部散落根目录 | 分类入 `scripts\`（197个） |
| docs 文档 | 40+ 历史报告混放 | 当前文档 + `docs\archive\`（35份归档） |
| 空目录 | dist / src / backups | 已删除 |
| game.html | 5,753,427 B | 5,750,915 B（清理1个重复定义） |
| 节点 / 死链 / 占位 | 2618 / 0 / 0 | 2618 / 0 / 0（**未受影响**） |

## 二、归档清单（backup\cleanup_20260907\，共 90 文件 / 19.38 MB）

| 子目录 | 数量 | 内容 |
|---|---|---|
| `scratch\` | 19 | 临时 node 检查产物（_chk_*.js，每次验证链会重新生成） |
| `preview\` | 10 | v33 配色/字体预览 html（6）与用户截图（4） |
| `py_baks\` | 55 | story 数据卷 .bak（与源文件同内容，归档） |
| （其余） | 6 | _test_syntax.js、_probe_seal2_out.txt 等零散临时文件 |

> 注：归档后 `backup\` 共 95 文件，含各版本 game.html 备份与本次 cleanup。

## 三、脚本整理（scripts\，共 197 个）

| 目录 | 数量 | 内容 |
|---|---|---|
| `scripts\rewrite\` | 89 | v31~v41 内容生成/占位重写脚本（_v4x_rw_*、_v40_rewrite_p*、_v36~39_*） |
| `scripts\fix\` | 35 | 修复/检查脚本（_fix_*、_check_*，含本轮 _fix_dup_ay5.py） |
| `scripts\probe\` | 33 | 剧情线探查脚本（_probe_*） |
| `scripts\util\` | 28 | 工具/数据文件（_find_*、_track_*、_dedup_nodes.py、_build_map.json 等） |
| `scripts\audit\` | 12 | 专项审计脚本（_audit_deep1~12、_audit_clean1） |

**验证链固定脚本保留根目录**（`_v39_extract.py` / `_check_dead_links.py` / `_audit_v41.py` / `_audit_deep4.py` / `_audit_deep13.py` / `_probe_ph4.py` / `_chk_quotes_gen.py`）。

## 四、文档整理（docs\）

- 保留：`QA_v41_检验报告.md`（最新）、`ARCHITECTURE.md`、`STYLE_GUIDE.md`、`项目结构说明.md`（新建）
- 归档至 `docs\archive\`：QA_v6~v40 共 34 份 + 旧版文档（DESIGN_v9 / NODE_INDEX / TOOLCHAIN_GUIDE_v29 / game_full / node_graph / 5 个 json 报告）
- 新建：`README_构建验证.md`（根目录，验证链说明）

## 五、代码层面检查（发现并修复 1 个真实问题）

- **重复节点 `academy_year5_intro`（2处定义）**：旧版（v39 引入）被新版毕业礼堂版覆盖，属无效冗余。已用括号配对法精确删除旧版（1176字节），保留生效版。
  - 修复过程中第一次正则误删 1.4MB → 立即从 `game_check.html` 恢复 → 改用配对法成功。
- **验证链复跑（删除后）**：
  - `node --check` 4/4 PASS
  - 死链 **0**（go引用 5988→5985，旧版被删引用的 3 个目标随之消失）
  - 节点 2618 不变；v37/v38 占位 0
- **localStorage 键**：读/写均为 `elda-qunxiong-v3-save`，未受任何移动/删除影响，存档兼容 ✓

## 六、验证结论

| 检查项 | 结果 |
|---|---|
| 语法（node --check ×4） | ✅ PASS |
| 死链 | ✅ 0 |
| 节点总数 | ✅ 2618 |
| 占位（v37/v38） | ✅ 0 |
| 存档键 | ✅ elda-qunxiong-v3-save |
| 核心文件完整性 | ✅ game.html 与 game_check.html 已同步 |

## 七、仍可优化（建议后续）

1. `story_elda_*.py` 数据卷（55个/约6.9MB）可考虑打包 zip 归档到 `backup\`，根目录只留构建链必需 —— 未执行，因它们是 `build_elda.py` 的源，删除会影响未来重建 game.html。
2. `docs\archive\` 若长期不查阅可再压缩为 zip。
3. 建议每完成一个大版本后运行本整理流程，保持根目录干净。
