# 结构台账 v58（工程基线快照）

> 生成：2026-09-08 · 权威源 `game.html`（4,443,887 字符 / 8,139,000+ 字节）· 工具 `tools/eldacheck`

## 一、总体规模

| 项 | 数值 | 口径 |
|---|---|---|
| script 数 | 6 | `<script>` 顶层块 |
| IIFE 数 | 70 | `(function(){` 匹配 |
| 严格模式块 | 0 | `'use strict'` 检测 |
| 节点定义 | 3,409 | `N["id"]=function` 全部出现（含重复定义） |
| 唯一节点（配平成功） | 3,263 | 括号配平 + 唯一 id |
| go 引用 | 7,052 | options 内 go 计数 |
| 死链 | 0 | go 目标缺失 |
| 注入 marker | 27 种 | 完整串（含子名） |
| 顶层隐式全局（既有模式） | 28 | 有引用、工作正常，建议后续收敛 |
| 顶层真泄漏（无引用） | 0 | 完全无引用 |

## 二、6 script 职责分布

| script | 职责 | 备注 |
|---|---|---|
| script0 | 样式 / 头部 / 基础 DOM | 最小 |
| script1 | 启动 / 初始化 / 读档骨架 | 入口 |
| script2 | v34/v40/v42 技术层 + 大量数据（COMBAT_TEXTS 等） | 历史脏数据集中地 |
| script3 | 引擎核心（writeNext/choose/render）+ N[] 节点主体 + v45-v48 引擎 | 节点主战场 |
| script4 | v48+ 职业系统 + v51-v57 各版引擎/数据/节点 | 增量最活跃 |
| script5 | 收尾 / 初始化调用 | 最小 |

## 三、注入 marker 全表（27 种）

| marker | 计数 | 用途 |
|---|---|---|
| /v48inj:crisis / know-* / mark-opts / marks / oaths / protectors / ritual | 各 1-5 | v48 破境/仪式扩展 |
| /v54inj:system | 1 | v54 强者分布 |
| /v55inj:arcA / arcB / arcC / skills | 各 1 | v55 命运弧 + 技能 |
| /v56inj:engine(+子) | 2 | v56 活态引擎 |
| /v57inj:data(+子) / engine(+子) / fix(+子) | 各 1-4 | v57 职业总成 |
| /v58eng:refhealth / savever | 各 1 | v58 引用健康 + saveVersion（本次新增） |

> 说明：`/v48inj:know-sense` 等 5 个、`/v57inj:fix:skillform` 等 3 个为**带子名的单批注入**（预留位注释），非重复注入。

## 四、命名空间与引用健康结论

- **健康模式**：`window.X = ...` 定义 + `window.X` 读取（STRONG_V53 / FACTION_PEERS_V53 / ORG_V52 等）——241 项 PASS。
- **顶层 let/const + 裸引用**（跨 script 全局词法环境可见，工作正常）：`S`（let）、`N`（const）、`RIVALS_V52`、`ErrorLog` —— `window.X` 形式不可见，本次已全部改为裸引用优先或 typeof 兜底。
- **本次修复的 12 项引用 bug**：
  1. `window.S&&S.worldState` ×7 → `typeof S!=='undefined'&&S&&S.worldState`（v53 will 触发条件此前静默失效）
  2. `window.v47_worldDelta` → `window.worldDelta`（真名，势力事件效果此前不落地）
  3. `window.v44_floatText` → `window.v44_showFloatText`（v55 施放飘字此前从未显示）
  4. `window.v44_toast` → `window.v44_pushToast`（v55 技能习得 toast 此前从未弹出）
  5. `window.ErrorLog` → typeof 兜底（catch 记录恢复）
  6. `window.ToastCenter` → typeof 兜底（未实现对象，行为不变）
  7. `window.unlockReading` → `window.v45_unlockReading`（v53 解锁桥此前失效）
  8. `window.v52_jobCn` 补导出（v57 桥恢复优先路径）
  9. ELDA.getNodes `window.N` → 裸 N 优先（兜底保留）
  10. RIVALS_V52 读取 → 裸引用优先（兜底保留）
- **saveVersion 写死 46 ×4 → 48**（v34 槽位保存 + saveGame 主函数）。

## 五、既有全局模式（28 项，建议后续收敛）

COMBAT_TEXTS / ENDINGS / COMBAT / renderStats / writePar（script2 旧引擎顶层赋值）；id / n / arr / curNode（script3）；sfxOn / ambNodes / applyEffects / effTarget / advanceDays / choose / writeNext / renderTop / renderStats 等（script4）。均为非严格模式隐式全局，工作正常；后续版本建议包 IIFE 或显式 window 导出。

## 六、后续注入规范（建议）

1. 新数据块：`const X = {...}` 顶层定义 + 同 script 裸引用，或 `window.X = {...}` + window 引用，**二选一，禁混用**。
2. 新引擎函数：IIFE 包裹 + 需跨 IIFE 暴露的用 `window.fnName = fnName` 显式导出。
3. 跨 IIFE 调用：一律 `window.导出名`；调用前 `typeof 检查` 或 `if(window.x)` 短路。
4. 新 marker：`/vXXinj:名称` 唯一；同批多段用 `:子名` 或 `-子名` 区分。

---

## 六、V58-ENG 收尾·任务二执行结果（2026-09-08）

**状态：已完成**。旧引擎顶层隐式全局 15 项原地 IIFE 包裹 + window 显式导出（marker /v58eng:iife/），IIFE 总数 70 → 85。

**包裹清单（15 项 / 15 个 IIFE）**：

| script | 名字 | 定义形态 | 行（包裹前） |
|---|---|---|---|
| script2 | COMBAT_TEXTS | 对象字面量 | 9233 |
| script2 | ENDINGS | 对象字面量 | 9276 |
| script2 | COMBAT | 对象字面量 | 35580 |
| script2 | renderStats | 赋值function（v21包装） | 49618 |
| script2 | writePar | 赋值function（v21包装） | 49625 |
| script4 | applyEffects | 赋值function（v44钩子） | 74155 |
| script4 | effTarget | 赋值function（v44钩子） | 74166 |
| script4 | advanceDays | 赋值function（v44钩子） | 74202 |
| script4 | choose | 赋值function（v44钩子） | 74208 |
| script4 | writeNext | 赋值function（v44钩子） | 74219 |
| script4 | renderTop | 赋值function（v44钩子） | 74223 |
| script4 | renderStats | 赋值function（v44钩子） | 74238 |
| script4 | showEnding | 赋值function（v44钩子） | 74252 |
| script4 | newGame | 赋值function（v44钩子） | 74270 |
| script4 | loadGame | 赋值function（v44钩子） | 74274 |

**包裹模板**：(function(){ /*v58eng:iife*/ <原语句>; window.X = X; })();——IIFE 原地放置、不移动代码、不加 'use strict'、不改变任何判定逻辑；函数体/调用点/内联事件引用一律不动（非严格模式裸引用向上查 window 的既有机制保持可见）。

**甄别排除（c_structure 误报，未包裹——包裹会破坏功能）**：
- curNode / sfxOn / ambNodes：实为 let 多变量声明（let storyEl, optEl, curNode=null / let AC=null, sfxOn=true, ambNodes=[]），c_structure 多变量声明只登记首个名字导致误报；
- id / n / arr：箭头函数参数（const \$ = id => ...、
nd = n => ...、choice = arr => ...）；
- skillId / isAoe / reason / success：函数参数默认值；
- type：SVG data-URI 字符串；target / tab：局部变量/HTML 属性。

**验证**：c_structure「既有全局模式」28 → 13（剩余 13 项全部为上述误报）；引用健康 FAIL 0 / PASS 253；浏览器抽查 15 项 window 导出全部 function/object 可见；choose/writeNext/advanceDays 等引擎判定链无感。
